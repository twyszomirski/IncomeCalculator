package pl.twyszomirski.service;

import java.math.BigDecimal;
import java.util.Date;

/**
 * Created by Tomasz
 * Service for fetching exchange rates
 */
public interface ExchangeRateService {

<<<<<<< Updated upstream
    BigDecimal getExchangeRate(String fromCode, String ToCode, String dateIdentifier) throws NoExchangeRateException;

    String getDateToken(Date date);
=======
    /**
     * Provides an exchange rate for given value pair and a date.
     * The concrete implementation should use ExchangeRateToken to cache the
     * reponses for a current day.
     * @param exchangeRateToken
     * @return
     * @throws NoExchangeRateException
     */
    Float getCurrentExchangeRate(ExchangeRateToken exchangeRateToken) throws NoExchangeRateException;
>>>>>>> Stashed changes
}
